<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--윤여훈 -->
<mapper namespace="hoondb">

<!-- sql문 작성 -->
<resultMap type="beanhoon.PurListVo" id="voMap"/>

	<!-- 생산요청서 리스트 조회 -->
	<select id='pur_list' resultMap="voMap" parameterType="beanhoon.PurListVo">
		select d.dCode, d.dCate, d.dName, to_char(d.dDate, 'yyyy.mm.dd') dDate, to_char(s.srlTerm, 'yyyy.mm.dd') srlTerm, d.dWrite 
		from document d join srList s 
		on d.dCode = s.srlCode where d.dStatus = 0 and d.dcate = #{dCate }
	</select>
	
	
	
</mapper>